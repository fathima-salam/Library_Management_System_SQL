<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Management System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #2c2c2c 0%, #1a1a1a 100%);
            min-height: 100vh;
            color: #e0e0e0;
        }

        .container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar Navigation */
        .sidebar {
            width: 250px;
            background: rgba(20, 20, 20, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
            border-right: 1px solid rgba(100, 100, 100, 0.3);
            padding: 2rem 0;
            position: fixed;
            height: 100vh;
            z-index: 100;
        }

        .logo {
            text-align: center;
            margin-bottom: 3rem;
            padding: 0 1.5rem;
        }

        .logo h1 {
            background: linear-gradient(135deg, #ffffff, #cccccc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 1.5rem;
            font-weight: 700;
        }

        .nav-menu {
            list-style: none;
            padding: 0 1rem;
        }

        .nav-item {
            margin-bottom: 0.5rem;
        }

        .nav-link {
            display: flex;
            align-items: center;
            padding: 1rem 1.5rem;
            text-decoration: none;
            color: #cccccc;
            border-radius: 12px;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .nav-link:hover {
            background: linear-gradient(135deg, #444444, #222222);
            color: white;
            transform: translateX(5px);
        }

        .nav-link.logout {
            background: linear-gradient(135deg, #666666, #333333);
            color: white;
            margin-top: 2rem;
        }

        .nav-link.logout:hover {
            transform: translateX(5px) scale(1.02);
            box-shadow: 0 5px 15px rgba(100, 100, 100, 0.3);
        }

        .nav-icon {
            margin-right: 0.75rem;
            font-size: 1.2rem;
        }

        /* Main Content */
        .main-content {
            margin-left: 250px;
            flex: 1;
            padding: 2rem;
            overflow-y: auto;
        }

        .hero-section {
            background: rgba(30, 30, 30, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 3rem;
            margin-bottom: 2rem;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            text-align: center;
            border: 1px solid rgba(100, 100, 100, 0.2);
        }

        .hero-title {
            font-size: 3rem;
            font-weight: 700;
            background: linear-gradient(135deg, #ffffff, #cccccc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 1rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
            color: #bbbbbb;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .books-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .book-card {
            background: rgba(40, 40, 40, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 1.5rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            overflow: hidden;
            border: 1px solid rgba(100, 100, 100, 0.2);
        }

        .book-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.5);
        }

        .book-image {
            width: 100%;
            height: 200px;
            background: linear-gradient(135deg, #666666, #333333);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem;
            position: relative;
            overflow: hidden;
        }

        .book-image::before {
            content: '';
            position: absolute;
            width: 80px;
            height: 120px;
            background: rgba(200, 200, 200, 0.9);
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .book-image::after {
            content: '📚';
            position: absolute;
            font-size: 2rem;
            z-index: 1;
        }

        .book-card h3 {
            font-size: 1.3rem;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 0.5rem;
        }

        .book-card p {
            color: #cccccc;
            line-height: 1.5;
            font-size: 0.95rem;
        }

        .reading-quotes {
            background: rgba(30, 30, 30, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(100, 100, 100, 0.2);
        }

        .quotes-title {
            text-align: center;
            font-size: 2rem;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 2rem;
        }

        .quote {
            background: linear-gradient(135deg, #555555, #333333);
            color: white;
            padding: 1.5rem;
            border-radius: 16px;
            margin-bottom: 1rem;
            font-style: italic;
            position: relative;
            border: 1px solid rgba(100, 100, 100, 0.3);
        }

        .quote::before {
            content: '"';
            font-size: 3rem;
            position: absolute;
            top: -10px;
            left: 10px;
            opacity: 0.5;
        }

        .quote-author {
            text-align: right;
            font-weight: 600;
            margin-top: 1rem;
            opacity: 0.9;
        }

        @media (max-width: 768px) {
            .sidebar {
                width: 200px;
            }
            
            .main-content {
                margin-left: 200px;
                padding: 1rem;
            }
            
            .hero-title {
                font-size: 2rem;
            }
            
            .books-gallery {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar Navigation -->
        <nav class="sidebar">
            <div class="logo">
                <h1>📖 LibraryMS</h1>
            </div>
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="/books" class="nav-link">
                        <span class="nav-icon">📚</span>
                        BOOKS
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/orders" class="nav-link">
                        <span class="nav-icon">📋</span>
                        ORDERS
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/users" class="nav-link">
                        <span class="nav-icon">👥</span>
                        USERS
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="nav-link logout">
                        <span class="nav-icon">🚪</span>
                        LOGOUT
                    </a>
                </li>
            </ul>
        </nav>

        <!-- Main Content -->
        <main class="main-content">
            <section class="hero-section">
                <h1 class="hero-title">Welcome to LibraryMS</h1>
                <p class="hero-subtitle">
                    Discover a world of knowledge at your fingertips. Our comprehensive library management system 
                    makes it easy to explore, borrow, and manage your favorite books. Join thousands of readers 
                    on their journey to knowledge and imagination.
                </p>
            </section>

            <section class="books-gallery">
                <div class="book-card">
                    <div class="book-image"></div>
                    <h3>Classic Literature</h3>
                    <p>Explore timeless masterpieces from renowned authors that have shaped literature throughout history. From Shakespeare to Dickens, discover stories that continue to inspire readers worldwide.</p>
                </div>
                
                <div class="book-card">
                    <div class="book-image" style="background: linear-gradient(135deg, #777777, #444444);"></div>
                    <h3>Modern Fiction</h3>
                    <p>Dive into contemporary narratives that reflect our current world. Experience compelling characters and thought-provoking themes in today's most celebrated novels.</p>
                </div>
                
                <div class="book-card">
                    <div class="book-image" style="background: linear-gradient(135deg, #888888, #222222);"></div>
                    <h3>Science & Technology</h3>
                    <p>Stay updated with the latest advances in science and technology. From quantum physics to artificial intelligence, expand your understanding of our evolving world.</p>
                </div>
                
                <div class="book-card">
                    <div class="book-image" style="background: linear-gradient(135deg, #999999, #555555);"></div>
                    <h3>History & Biography</h3>
                    <p>Journey through time and learn from the experiences of remarkable individuals. Discover untold stories and gain insights into the events that shaped our world.</p>
                </div>
            </section>

            <section class="reading-quotes">
                <h2 class="quotes-title">Words of Wisdom</h2>
                
                <div class="quote">
                    A reader lives a thousand lives before he dies. The man who never reads lives only one.
                    <div class="quote-author">— George R.R. Martin</div>
                </div>
                
                <div class="quote" style="background: linear-gradient(135deg, #666666, #444444);">
                    The more that you read, the more things you will know. The more that you learn, the more places you'll go.
                    <div class="quote-author">— Dr. Seuss</div>
                </div>
                
                <div class="quote" style="background: linear-gradient(135deg, #777777, #333333);">
                    Books are a uniquely portable magic.
                    <div class="quote-author">— Stephen King</div>
                </div>
            </section>
        </main>
    </div>

    <script>
    // Sidebar navigation
    document.querySelectorAll('.nav-link').forEach(link => {
        link.addEventListener('click', function(e) {
            // If it's logout, prevent default and confirm
            if (this.classList.contains('logout')) {
                e.preventDefault();
                if (confirm('Are you sure you want to logout?')) {
                    window.location.href = '/logout'; // redirect to logout route
                }
                return;
            }

            // For normal links, allow navigation
            // Highlight active link
            document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));
            this.classList.add('active');
        });
    });

    // Book card hover effects
    document.querySelectorAll('.book-card').forEach(card => {
        card.addEventListener('mouseenter', function() {
            this.style.transform = 'translateY(-10px) scale(1.02)';
        });

        card.addEventListener('mouseleave', function() {
            this.style.transform = 'translateY(0) scale(1)';
        });
    });

    // Simulate page load message
    setTimeout(() => {
        console.log('Library Management System loaded successfully!');
    }, 1000);
</script>
</body>
</html>